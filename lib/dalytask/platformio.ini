[env]
# Daly task currently uses BLE classes bundled with the ESP32 Arduino framework.
# Keep this file so the additive user-task structure remains explicit; add
# external dependencies here only if a later NimBLE migration is required.
